[package]
name = "knowledge-graph"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
katgpt-core = { workspace = true, features = [
    "sense_composition",
    "schema_centroid",
    "bake_precision",
    "merkle_octree",
] }
katgpt-types = { git = "https://github.com/lidm0707/katgpt-rs", branch = "main" }
blake3 = "1"
fastrand = "2"
